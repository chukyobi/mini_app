import React from 'react';
import { ScratchCardProps } from './types';
export declare const ScratchCard: ({ width, height, image, finishPercent, onComplete, brushSize, children, }: ScratchCardProps) => React.JSX.Element;
